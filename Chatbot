#include <stdio.h>
#include <string.h>

int main() {
    char input[100];

    printf("🤖 Hello! I’m ChatBot.\n");
    printf("Type something (type 'bye' to exit)\n\n");

    while (1) {
        printf("You: ");
        gets(input); // to take input (unsafe but simple for this project)

        if (strcmp(input, "hi") == 0 || strcmp(input, "hello") == 0) {
            printf("Bot: Hello there! 😊\n");
        } 
        else if (strcmp(input, "how are you") == 0) {
            printf("Bot: I'm doing great! How about you?\n");
        } 
        else if (strcmp(input, "i am fine") == 0) {
            printf("Bot: Glad to hear that! 😄\n");
        } 
        else if (strcmp(input, "what is your name") == 0) {
            printf("Bot: I’m your friendly ChatBot made in C.\n");
        } 
        else if (strcmp(input, "bye") == 0) {
            printf("Bot: Goodbye! Have a nice day! 👋\n");
            break;
        } 
        else {
            printf("Bot: Sorry, I didn’t understand that.\n");
        }
    }

    return 0;
}
